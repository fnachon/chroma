[pytest]
markers =
    integration: end-to-end tests that rely on packaged artifacts, model weights, or external-style workflows.
    slow: longer-running tests intended for comprehensive CI or release validation.
